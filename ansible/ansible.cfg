[defaults]
inventory = inventory.ini
interpreter_python = auto_silent

[ssh_connection]
# Freshly cloned VMs have new host keys every time; accept them on
# first contact instead of prompting.
ssh_args = -o StrictHostKeyChecking=accept-new
